<% provide(:title, @user.name) %>
<dev class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user, size: 100 %>
				<%= @user.name %>
				<%= "administrator!" if @user.admin %>
			</h1>
		</section>
	</aside>
</dev>